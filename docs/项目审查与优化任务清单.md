# 项目质量优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复会造成提醒漏发、数据失真和错误空态的高风险问题，并建立可重复执行的质量门禁与后续优化任务清单。

**Architecture:** 保持现有 React 18 + Taro 4.2 + Zustand + 微信云开发架构，不引入新的业务域。先修复提醒闭环和数据一致性，再补齐加载/错误状态、测试与工程配置，最后处理可访问性、性能和维护性问题。

**Tech Stack:** React 18、TypeScript 5.4/5.x、Taro 4.2、Zustand 5、Taroify、Sass、微信云开发、Node.js 云函数。

## Global Constraints

- 默认使用中文文案，保持当前“愈历”产品语气。
- 不主动增加业务能力；健康指标继续限定为删除闭环、轻量排序和稳定展示，不增加分类或拖拽排序。
- 不重构无关文件；每个任务应能独立验证和回滚。
- 前端改动必须覆盖 loading、empty、error；输入改动必须包含校验和明确错误提示。
- 单文件不得超过 500 行。
- 除质量门禁任务中明确说明的 TypeScript 执行器外，不新增依赖；如现有工具可以完成目标，应优先复用。
- 涉及云端数据迁移时，先备份、统计影响范围，再灰度执行；不得把本地 `database/` 导出文件提交到仓库。

---

## 1. 审查范围与验证基线

### 1.1 已审查范围

- 根配置与工程化：`package.json`、`pnpm-lock.yaml`、`tsconfig.json`、Taro/Vite/Babel、ESLint、Stylelint、Husky。
- 主包页面与组件：`src/pages/**`、`src/components/**`、`src/hooks/**`、`src/store/**`、`src/services/**`、`src/utils/**`。
- 健康分包：`src/subpackages/health/**`。
- 云端：`cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/**`。
- 规格与文档：`openspec/**`、`docs/**`、`README.md`。
- 本地遗留数据样本：被 `.gitignore` 排除的 `database/database_export-xDpkALX7viJg.json`，仅用于确认历史字段形态。

仓库当前约有 360 个跟踪文件，TypeScript/TSX/云函数 JS 合计约 13,865 行。未对微信云开发线上环境、真实订阅消息额度和真机无障碍行为作运行态结论。

### 1.2 当前验证结果

| 检查                           | 结果           | 证据/说明                                                                                               |
| ------------------------------ | -------------- | ------------------------------------------------------------------------------------------------------- |
| TypeScript（带历史 CLI 覆盖）  | 通过           | `tsc --noEmit --ignoreDeprecations 5.0 --skipLibCheck true` 退出 0                                      |
| TypeScript（直接读取仓库配置） | 失败           | `tsconfig.json:8` 的 `ignoreDeprecations: "6.0"` 被当前 TypeScript 5.x 拒绝，报 TS5103                  |
| 分享产物检查                   | 通过但覆盖不足 | 当前 `dist` 通过 `scripts/verify-share-flags.mjs`，但脚本未覆盖健康分包，也不保证产物新鲜               |
| ESLint                         | 不可运行       | `.eslintrc:7` 的 `jsx-quotes` 参数 `double` 非法，配置加载即退出 2                                      |
| Stylelint                      | 基线不可用     | 当前配置不理解 Sass `@use`、变量和 BEM 命名，产生 632 个错误，不能作为有效门禁                          |
| 三个 `*.verify.ts`             | 未接入         | `package.json` 无执行脚本；直接使用 Node 22 运行会因 TS 扩展名/模块解析失败；健康指标 verify 还未自执行 |
| 工作区                         | 干净           | 审查开始前 `git status --short` 无输出                                                                  |

### 1.3 优先级定义

- **P0：** 会直接导致核心提醒静默漏发，必须在继续扩展功能前处理。
- **P1：** 会造成数据失真、重复写入、错误成功反馈、隐私暴露或关键交互不可用，建议下一迭代完成。
- **P2：** 影响可维护性、规模化、可访问性和交付稳定性，应进入近期技术债迭代。
- **P3：** 低风险清理与结构优化，在 P0/P1 稳定后处理。

---

## 2. P0：核心提醒闭环

### Task P0-01：修复药品过期提醒调度与扫描条件互相矛盾

**Files:**

- Modify: `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/expiryReminder/config.json:5`
- Modify: `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/expiryReminder/index.js:276`
- Test: 新增 `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/expiryReminder/index.test.js`

**证据：** 定时器当前仅每周三 09:00 执行（`config.json:9`），函数却只查询 `expiryDate === today + N`（`index.js:279-307`），因此非特定星期到期的药品永远不会成为候选。同时函数仍按每条药品的 `remindTime` 判断（`index.js:344-349`），但每周只在 09:00 运行一次，其他时间也永远无法命中。

**Interfaces:**

- Consumes: `expiryDate`、`ENV_EXPIRY_ADVANCE_DAYS`、药品提醒时刻。
- Produces: 每天所有到期候选都至少被扫描一次，并在用户配置的时间窗口内最多发送一次。

- [x] 将过期提醒定时器调整为与时间窗口一致的周期；若保留用户时刻，采用每 30 分钟执行一次，而不是固定每周一次。
- [x] 抽出纯函数 `isExpiryReminderDue({ now, expiryDate, advanceDays, remindTime })`，统一判断日期、时间和跨午夜窗口。
- [x] 保留 `lastWechatExpiryReminderDate` 防重，但为“日期不命中”和“时间不命中”分别统计原因。
- [x] 用周一、周三、周日到期样本验证都能成为候选，并覆盖 08:30、09:00、21:30 和跨午夜时刻。

**Acceptance:**

- 任意星期到期的药品都不会因定时器星期限制被漏掉。
- 任何允许配置的提醒时刻都存在至少一个可命中的调度窗口。

### Task P0-02：兼容缺少 `reminderEnabled` 的历史药品，避免静默漏发

**Files:**

- Modify: `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/reminder/index.js:301`
- Modify/Create: 受控数据迁移脚本，放入 `scripts/migrations/` 并默认 dry-run
- Test: 新增提醒候选筛选测试

**证据：** 客户端迁移将缺字段记录视为 `reminderEnabled=true`（`src/services/reminder.ts:123`），云函数查询却要求数据库字段物理存在且等于 `true`（`reminder/index.js:302-309`）。本地历史导出样本中存在 5 条 active 记录且均缺少该字段。

**Interfaces:**

- Consumes: 旧记录的 `status`、历史字段和当前用户归属。
- Produces: 迁移后的文档明确包含 `reminderEnabled`，过渡期查询不会漏掉旧记录。

- [ ] 编写 dry-run 迁移脚本，只统计缺字段记录数量、用户数和目标默认值，不打印药名、openid 或完整文档。
- [ ] 备份后将历史 active/paused 药品补齐显式 `reminderEnabled`；默认值必须与客户端当前兼容语义一致。
- [ ] 在迁移完成前，云函数候选查询需兼容缺字段记录；迁移完成并验证后再收紧查询。
- [ ] 增加“缺字段旧记录 + 显式 true 新记录 + 显式 false 记录”测试。

**Acceptance:**

- 缺字段旧记录和显式 true 记录在命中日期/时刻时都进入候选。
- 显式 false 记录不会发送。
- 迁移可重复执行且不会重复修改已规范记录。

---

## 3. P1：数据一致性、可靠性与关键体验

### Task P1-01：按订阅模板分别记录和消费授权，并统一模板配置来源

**Files:**

- Modify: `src/constants/index.ts:132`
- Modify: `config/index.ts:44`
- Modify: `types/global.d.ts:14`
- Modify: `src/services/wechatReminder.ts:18`
- Modify: `src/services/auth.ts:6`
- Modify: `src/pages/profile/index.tsx:47`
- Modify: `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/auth/index.js:21`
- Modify: 三个提醒云函数的订阅资格读写逻辑

**证据：** 前端分别获得三个模板的授权结果（`wechatReminder.ts:96-102`），随后压缩为一个用户级 `available/consumed`；三个云函数都读取并消费同一个字段。任一提醒发送后，其余已授权模板也会被阻断。客户端模板 ID 又硬编码在 `constants/index.ts:148-152`，云函数则分别读取三个环境变量，存在配置漂移。

**Interfaces:**

- Produces: `wechatTemplateSubscriptions: Record<TemplateKind, SubscriptionState>`，其中 `TemplateKind = 'medicine' | 'expiry' | 'checkup'`，`SubscriptionState = 'unknown' | 'available' | 'consumed' | 'rejected'`。

- [ ] 将三个模板 ID 都通过构建环境注入客户端，删除源码中的具体 ID；缺失任一配置时显示对应模板配置错误。
- [ ] 前端保留每个模板的 decision，允许“部分接受、部分拒绝”，不得用 `allAccepted` 丢弃已接受授权。
- [ ] auth 云函数只接受白名单模板键和状态，按模板合并更新。
- [ ] 三个发送云函数只读取、消费自己的模板状态。
- [ ] 旧的单一 `wechatSubscriptionStatus` 仅用于迁移；不能把一次旧 `available` 扩散成三个可用授权，迁移后应要求用户重新授权。
- [ ] 资料页分别呈现三类提醒的可用/已消费/未授权状态，保持界面轻量，不增加新的管理层级。

**Acceptance:**

- 同时接受三个模板后，分别触发三类提醒，三者只消费自己的授权。
- 只接受一个模板时，该模板可发送，另两个不会假装已授权。
- 客户端请求的模板 ID 与云函数发送的模板 ID 来自同一套部署配置。

### Task P1-02：统一东八区时间计算并修复跨午夜匹配

**Files:**

- Modify: 三个提醒云函数中的 `formatDate`、`formatTime`、`isTimeMatched`
- Create: `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/shared/reminderTime.js`
- Test: `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/shared/reminderTime.test.js`

**证据：** 日志时间显式转换为 UTC+8，但真正匹配使用 `new Date().getHours()/getMinutes()`；部署环境不是东八区时会偏移。当前绝对值比较也无法识别 23:50 与次日 00:10 相差 20 分钟。

- [ ] 用注入的 `Date` 计算固定时区的日期和分钟数，业务函数不得直接读取宿主机本地小时。
- [ ] 用环形 24 小时距离计算容差：`min(abs(a-b), 1440-abs(a-b))`。
- [ ] 三个云函数共用同一实现，删除各自复制的时间匹配函数。
- [ ] 在 `TZ=UTC` 与 `TZ=Asia/Shanghai` 下运行同一组样例，结果必须一致。

**Acceptance:** 09:00、23:50/00:10、无效时刻、夏令时无关环境均得到确定结果。

### Task P1-03：为定时发送建立并发防重与可观测失败语义

**Files:**

- Modify: 三个提醒云函数的扫描、发送、状态更新流程
- Create: `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/shared/deliveryClaim.js`
- Test: 并发执行与数据库失败测试

**证据：** 当前先读取 `lastWechatReminderDate`，发送后才更新；两个调度实例并发时都可通过检查并重复发送。整批数据库扫描异常又被 catch 后正常 return，调度平台会看到“成功但发送 0 条”。

- [ ] 为每个“用户 + 模板 + 业务记录 + 计划日期”建立稳定 delivery key。
- [ ] 发送前使用事务或带租约的原子 claim，已被其他实例 claim/sent 的任务直接跳过。
- [ ] 明确失败状态与可重试策略；外部 API 成功但状态落库失败时不得无声重发。
- [ ] 单条业务失败进入 `failed` 统计；整批扫描、权限或索引失败必须重新抛出，使平台告警/重试生效。
- [ ] 日志只记录 delivery key 后缀、结果和错误码，不记录药名、检查标题或 openid。

**Acceptance:** 两个实例并发处理同一任务时外部发送调用最多一次；数据库扫描失败时云函数退出非 0。

### Task P1-04：修复“稍后盘点”被普通编辑错误清除

**Files:**

- Modify: `src/components/MedicineComposer/useMedicineComposerForm.ts:76`
- Modify: `src/components/MedicineComposer/InventorySection.tsx`
- Test: `src/utils/medicineInventory.verify.ts` 或新的表单 payload 测试

**证据：** `markDone(..., { inventoryLater: true })` 保留旧数量并设置 `inventoryNeedsCalibration=true`；编辑表单会预填旧数量，任何保存都会写回 `inventoryNeedsCalibration:false`（`useMedicineComposerForm.ts:384-418`）。只改名称也会让旧库存重新变成有效基准。

- [ ] 待盘点状态下不把旧基准数量当作可提交的新盘点数量。
- [ ] 只有用户显式输入合法数量和日期时才清除 `inventoryNeedsCalibration`。
- [ ] 普通字段编辑时保留待盘点标志、旧基准和提示。
- [ ] 增加“稍后盘点 → 只改备注 → 保存”和“稍后盘点 → 完成新盘点”两个回归用例。

**Acceptance:** 普通编辑后仍显示待盘点；完成有效盘点后才恢复余量估算。

### Task P1-05：消除检查完成/重新安排的重复提交和跨端覆盖

**Files:**

- Modify: `src/components/CheckupCompletionSheet/index.tsx:87`
- Modify: `src/components/CheckupRestartSheet/index.tsx:91`
- Modify: `src/store/checkupStore.ts:124`
- Modify: 对应云端写入接口
- Test: 检查状态 mutation 并发测试

**证据：** 两个 Sheet 均未使用现有 `useSubmissionGuard`，请求期间按钮和关闭路径仍可操作；store 基于旧快照整体覆盖 `completionHistory`，跨端并发可能重复或丢失记录。

- [x] 两个 Sheet 复用 `useSubmissionGuard`，按钮显示 loading/disabled，并向 BottomSheet 传 `closeDisabled`。
- [x] 完成事件使用稳定事件 ID，在云端用事务、版本检查或原子追加更新历史。
- [x] 冲突时拉取新版本并给出“记录已更新，请确认后重试”，不得静默覆盖。
- [x] 覆盖快速点击 5 次、两个 Promise 并发和两条不同完成事件并发。

**Acceptance:** 同一完成动作只写一次；不同完成事件不会互相覆盖；失败后输入保留且可重试。

**部署：** 发布前需在微信云开发环境中部署 `checkupMutation` 云函数及其依赖；客户端不会回退到直接更新检查状态的旧写入路径。

### Task P1-06：修复健康指标同日竞态、静默成功和部分成功误报

**Files:**

- Modify: `src/subpackages/health/services/healthMetric.ts:198`
- Modify: `src/subpackages/health/services/healthMetric.ts:256`
- Modify: `src/subpackages/health/store/healthMetricStore.ts:188`
- Test: `src/subpackages/health/utils/healthMetricUtils.verify.ts` 及 service/store 测试

**证据：** 同日记录采用“先查后新增”，并发请求可各自新增；更新/删除指标找不到云文档时直接 return，store 仍提交本地成功；保存记录成功后更新默认范围失败时，整体进入失败分支但记录已在云端，用户重试会遇到重复确认。

- [ ] 用“用户 + metricTypeId + date”的确定性键、唯一约束或云端事务实现同日 upsert。
- [ ] 提交锁必须在首次异步查重前生效。
- [ ] `update/delete` 找不到文档或 `stats.updated !== 1` 时抛出明确 not-found，不得本地假成功。
- [ ] 将“保存记录”和“保存为默认范围”返回拆成明确结果；记录已保存但默认更新失败时，本地提交记录并显示部分成功提示，只重试默认设置。
- [ ] 用两个并发保存、另一端已删除、默认设置更新失败三个用例验证。

**Acceptance:** 同一用户/指标/日期最终只有一条记录；云端缺失时不显示成功；任何部分成功都与刷新后的云端状态一致。

### Task P1-07：补齐启动、检查提醒和每日状态的 loading/error/retry

**Files:**

- Modify: `src/app.ts:19`
- Modify: `src/store/checkupStore.ts:31`
- Modify: `src/store/healthStatusStore.ts:26`
- Modify: `src/hooks/useCheckups.ts`
- Modify: `src/hooks/useHealthStatus.ts`
- Modify: `src/pages/home/index.tsx:296`
- Modify: `src/pages/list/index.tsx:345`
- Modify: `src/pages/profile/index.tsx:314`
- Modify: `src/subpackages/health/pages/health-status-records/index.tsx:130`

**证据：** reminderStore 有 loading/error，但 checkupStore 和 healthStatusStore 没有；冷启动、慢网或失败时空数组会直接渲染为“还没有提醒/0 个/没有记录”，形成数据丢失错觉。`Promise.all` 中任一未捕获失败还会让启动流程整体 reject。

- [ ] 两个 store 增加 `loading`、`error`、`lastLoadedAt`，失败时保留已有数据。
- [ ] 启动加载使用独立错误隔离；一个域失败不能阻断其他域完成。
- [ ] 首次加载显示 loading/skeleton；无数据且加载成功才显示 empty；失败显示 error + retry。
- [ ] 有旧数据的刷新失败显示非阻塞提示，不清空内容。
- [ ] 统一列表必须能表达药品成功但检查失败的部分加载状态。

**Acceptance:** 3 秒慢网、登录失败、单域失败、全部失败、恢复重试均不会误显示真实空数据。

### Task P1-08：修复历史数据迁移的枚举与日期保真

**Files:**

- Modify: `src/services/reminder.ts:26`
- Modify: `src/services/checkup.ts:21`
- Modify: 日期迁移辅助函数
- Test: 增加历史样本 round-trip 测试

**证据：** `MedicineSchedule` 与表单选项包含“无所谓”，`reminder.ts` 白名单遗漏它并 fallback 为“饭后”。历史导出还存在日期对象形态，而迁移层直接透传，`parseDate` 最终对值调用 `split('-')`。

- [ ] 从共享常量派生 schedule 校验集合，保证所有 `SCHEDULE_OPTIONS` 保存再加载后不变。
- [ ] 把字符串、Date 和云数据库扩展日期统一转换为 `YYYY-MM-DD`；无效日期进入明确错误/隔离路径，不默认为今天掩盖脏数据。
- [ ] 规范化历史日期数组，只保留合法日期字符串并记录被隔离数量。
- [ ] 用全部枚举值和本地历史导出中的日期形态做测试，禁止输出敏感原始数据。

**Acceptance:** “无所谓”刷新后仍为“无所谓”；迁移后的所有业务日期均为合法字符串，派生函数不崩溃。

### Task P1-09：修复关键输入边界和身份未就绪操作

**Files:**

- Modify: `src/components/MedicineComposer/index.tsx:323`
- Modify: `src/components/MedicineComposer/useMedicineComposerForm.ts:354`
- Modify: `src/pages/profile/index.tsx:121`

**证据：** 最近开药日期 Picker 无 `end={today()}`，提交层也未拒绝未来日期；身份未就绪时头像路径会生成 `avatars/null.<ext>`，头像和昵称操作仍可触发。

- [ ] 最近开药日期 Picker 限制到今天，并在提交层再次校验 `currentPrescriptionDate <= today()`。
- [ ] 头像上传前同步校验 userId；未登录/登录中禁用头像和昵称更新并提供恢复提示。
- [ ] 空昵称失焦时恢复已保存值并显示字段错误，设置与微信昵称场景相容的 `maxLength`。
- [ ] mock 未来日期与 `getUserId() === null`，确认没有云端调用。

**Acceptance:** 伪造未来日期无法保存；未登录头像不会上传到共享路径；昵称 UI 与云端状态一致。

### Task P1-10：清理敏感健康数据日志并定义最小可观测字段

**Files:**

- Modify: `src/app.ts`、`src/services/auth.ts`、`src/services/reminder.ts`、`src/services/wechatReminder.ts`
- Modify: 四个云函数中的日志语句
- Create: 轻量日志字段约定文档 `docs/logging-and-privacy.md`

**证据：** 当前客户端输出 openid、完整药品数组、昵称和头像；云函数输出完整用户记录、药名和检查标题。该项目的数据属于健康相关敏感信息。

- [ ] 删除生产环境中的原始对象日志。
- [ ] 允许记录的字段限定为匿名 delivery key 后缀、数量、耗时、错误码和阶段。
- [ ] 开发日志也不得输出 openid、药名、昵称、头像 URL、备注和检查标题。
- [ ] 增加静态扫描命令，阻止已知敏感字段进入 `console.*` 模板。

**Acceptance:** 生产构建执行登录、加载、发送流程时，日志中不出现任何用户标识或健康内容。

### Task P1-11：建立真正可执行的 TypeScript、ESLint、Stylelint 和逻辑验证门禁

**Files:**

- Modify: `tsconfig.json:8`
- Modify: `.eslintrc:6`
- Modify: `stylelint.config.mjs`
- Modify: `package.json:13`
- Modify: 三个 `*.verify.ts`
- Create: `.github/workflows/verify.yml`（若仓库启用 GitHub Actions）

**证据：** 直接 `tsc --noEmit` 因 `ignoreDeprecations: "6.0"` 失败；ESLint 配置本身非法且与现有单引号风格冲突；Stylelint 未按 Sass/BEM 配置；verify 文件无统一执行入口，健康指标 verify 只导出不执行。

**Interfaces:**

- Produces: `pnpm run typecheck`、`pnpm run lint`、`pnpm run lint:styles`、`pnpm run verify:logic`、`pnpm run verify`。

- [ ] 将 `ignoreDeprecations` 改为当前 TypeScript 5.x 接受的 `"5.0"`，使直接 typecheck 与编辑器一致。
- [ ] ESLint 以仓库现有单引号为基线，修正 `jsx-quotes` 合法参数；lint 命令默认不得带 `--fix`。
- [ ] Stylelint 配置 Sass 解析、允许项目 BEM 命名和 Sass at-rule，再逐项处理真实样式错误。
- [ ] 用一个明确的 TypeScript 执行器接入三个 verify；如现有工具无法可靠解析 TS/路径别名，可新增且只新增 `tsx` 这一项轻量 devDependency，并在提交说明中写明原因。
- [ ] `verifyHealthMetricUtils()` 必须真正执行；破坏任一断言时命令必须非 0。
- [ ] `pnpm run verify` 按顺序组合 typecheck、只读 lint、样式检查、逻辑验证和分享检查。

**Acceptance:** 全新安装后单条 `pnpm run verify` 可重复执行；任一子检查失败都会阻止通过；命令不修改源码。

### Task P1-12：统一 750 设计坐标下的触控目标与基础字号

**Files:**

- Modify: `src/assets/styles/variables.scss:1`
- Modify: `src/components/BottomSheet/index.scss:65`
- Modify: 卡片、筛选、表单和健康状态相关 SCSS
- Test: 375/414 宽微信真机或开发者工具尺寸核验

**证据：** 项目 `designWidth: 750` 且启用 pxtransform。`52px` 最终约为 26 逻辑像素，`72px` 约为 36 逻辑像素；`$text-xs/$text-sm/$text-base = 18/20/22px` 最终约为 9/10/11px。多个关闭、筛选、chip 和卡片操作低于 44×44 逻辑像素。

- [ ] 增加设计坐标语义 token：主要触控目标至少 88px（对应 44 逻辑像素），相邻目标逻辑间距至少 8px。
- [ ] 不强制放大视觉图标，可通过外层 padding 扩大点击区。
- [ ] 正文目标约 32rpx，辅助文字不低于约 28rpx；关键药品、日期和错误提示支持系统字体放大。
- [ ] 优先修复 BottomSheet 关闭、筛选、Medicine/Checkup 卡片操作和长表单按钮。

**Acceptance:** 375/414 宽下主要交互不小于 44×44 逻辑像素，正文约不低于 16px、辅助信息约不低于 14px，放大字体后关键内容不截断。

---

## 4. P2：规模化、交付稳定性与可访问性

### Task P2-01：为业务列表和定时扫描增加分页

**Files:**

- Modify: `src/services/reminder.ts:137`
- Modify: `src/services/checkup.ts:74`
- Modify: `src/services/healthStatus.ts:51`
- Modify: `src/subpackages/health/services/healthMetric.ts:86`
- Modify: 三个提醒云函数的 `.limit(1000)` 扫描

**证据：** 客户端多处固定 limit 100，全局定时扫描固定 limit 1000，超出部分会静默截断。

- [ ] 用稳定排序字段 + `_id` 游标分页，禁止仅用 `skip` 扫描大集合。
- [ ] 云函数先按当天候选条件缩小范围，再按批次扫描并输出批次统计。
- [ ] 双字段兼容查询去重后仍需保证读取完整；记录当前迁移阶段，完成 `_openid/userId` 统一后移除双查。
- [ ] 用 limit+1 和 2×limit+1 数据验证最后一条仍会加载/发送。

**Acceptance:** 数据量超过当前 limit 时无静默丢失，分页失败会返回明确错误。

### Task P2-02：修复分享校验的路由覆盖和陈旧产物假阳性

**Files:**

- Modify: `scripts/verify-share-flags.mjs:4`
- Modify: `package.json`
- Test: 主包与健康分包分享配置负例

**证据：** 脚本硬编码主包页面，包含已不存在的 checkups 并静默跳过，未覆盖两个健康分包；只读取已有 `dist`，不确认产物是否来自当前源码。

- [ ] 从共享路由清单生成主包与分包页面列表，缺失页面不得静默 continue。
- [ ] 分享验证前执行新鲜构建，或比较产物时间/commit 标记并拒绝陈旧 dist。
- [ ] 删除任一主包或分包分享标志时，验证必须失败。

**Acceptance:** 所有真实页面均被检查，不存在的历史路由不再掩盖问题。

### Task P2-03：补齐环境、包管理器和云函数依赖的可复现契约

**Files:**

- Modify: `package.json`
- Create: `.env.example`
- Modify: `README.md`
- Modify: `types/global.d.ts`
- Modify: 四个云函数部署单元的 lockfile/版本策略

**证据：** 根项目有 pnpm 锁文件但未声明 `packageManager`/`engines`，dev 脚本内部混用 npm；仓库没有可提交的环境变量示例；云函数独立使用范围依赖但没有各自 lockfile。

- [ ] 声明经过验证的 Node 与 pnpm 版本，dev 脚本统一使用当前包管理器。
- [ ] `.env.example` 只列变量名和安全占位值，覆盖 cloud ID 与三个模板 ID；不得提交 APP_SECRET。
- [ ] README 写明安装、开发、构建、云函数环境变量、部署和验证命令。
- [ ] 为每个独立云函数部署单元冻结依赖，执行依赖审计并单独回归微信 API 调用。
- [ ] 明确 `.env.test` 的真实支持策略；未实现 test 加载时删除误导性模板。

**Acceptance:** 全新环境能仅依赖 README 和仓库声明完成冻结安装、构建与云函数部署。

### Task P2-04：补齐表单字段级错误、未保存保护和语义状态

**Files:**

- Modify: `MedicineComposer`、`CheckupComposer`、`HealthStatusComposer`
- Modify: `BottomSheet`
- Modify: `MedicineCard`、筛选控件、`ProgressBar`

**证据：** 长表单只用短暂 Toast 报错；修改后点击遮罩/关闭会直接丢弃；MedicineCard 自定义按钮无 role/label/disabled，筛选/chip 缺 selected 语义，进度条无 progressbar 语义。

- [ ] 保留 Toast 作为概要，同时在字段下显示持久错误，设置 `aria-invalid` 和可播报错误区域。
- [ ] 提交失败滚动/聚焦首个错误；修正字段后清除对应错误。
- [ ] 为长表单维护 dirty 状态，遮罩、关闭、取消和系统返回统一经过未保存确认。
- [ ] 优先使用 Taro/Taroify 原生 Button/Switch；自定义控件补齐 role、label、pressed/checked/disabled。
- [ ] ProgressBar 提供当前值和可理解的业务文本，不能只靠颜色表达风险。

**Acceptance:** VoiceOver/微信无障碍检查能播报错误、选中、禁用和进度；修改中的表单不会无提示丢失。

### Task P2-05：同步并归档已完成的 OpenSpec 变更，修正文档漂移

**Files:**

- Verify/Archive: `openspec/changes/add-list-refresh-incremental-rendering/`
- Verify/Archive: `openspec/changes/add-medicine-inventory-estimation/`
- Verify/Archive: `openspec/changes/merge-checkups-into-reminders/`
- Modify: 对应 `openspec/specs/**/spec.md`
- Modify: `docs/3.开发计划.md:251`、`README.md`

**证据：** 三个 change 的 tasks 均已勾选，但仍留在 active changes；开发计划仍有“本地存储模拟/退出恢复”的旧表述，README 只描述 OpenSpec 命令，缺少当前项目使用说明。

- [ ] 对每个 change 先运行 `openspec-verify-change`，确认实现与任务一致。
- [ ] 将已接受的 delta spec 同步到主 spec，再运行 `openspec validate --all --json`。
- [ ] 验证通过后使用 `openspec-archive-change` 逐个归档，不能直接移动目录绕过同步。
- [ ] 更新开发计划中已被云数据库替代的本地存储描述，并让 README 反映当前四 Tab、统一提醒中心和健康分包。

**Acceptance:** active changes 只保留真正未完成工作；主 spec、归档记录、代码和产品文档一致。

### Task P2-06：逐步收紧云端数据边界类型，消除显式 `any`

**Files:**

- Modify: `tsconfig.json:10`
- Modify: `src/services/reminder.ts:92`
- Modify: `src/services/checkup.ts:42`
- Modify: 各 service 的云数据库响应类型和迁移函数

**证据：** `noImplicitAny` 当前为 false，药品/检查迁移与 map 回调多处使用显式 `any`；这会让历史脏数据直到渲染或计算阶段才暴露。

- [ ] 先把云数据库原始记录定义为 `unknown` 或窄化后的 `CloudRecord`，所有字段在迁移边界校验。
- [ ] 为迁移函数增加非法数字、数组、状态、日期和空 ID 用例。
- [ ] 清理当前源码的隐式 any 后开启 `noImplicitAny: true`；暂不一次性打开与本任务无关的全部 strict 子项。
- [ ] 保持 Taro/云 SDK 外部类型断言集中在 service 边界，不向 store 和组件传播。

**Acceptance:** `noImplicitAny: true` 下 typecheck 通过，迁移层面对畸形云记录返回明确、可测试的结果。

---

## 5. P3：维护性与低风险清理

### Task P3-01：拆分超长药品表单 Hook

**Files:**

- Modify: `src/components/MedicineComposer/useMedicineComposerForm.ts:1`（当前 501 行）
- Create: `src/components/MedicineComposer/validateMedicineForm.ts`
- Create: `src/components/MedicineComposer/buildMedicinePayload.ts`

- [ ] 只抽取纯校验和 payload 构建，不改变组件状态模型或业务规则。
- [ ] 为抽出的纯函数覆盖未来日期、待盘点、剂量变更和提醒关闭场景。
- [ ] 保持每个文件不超过 500 行。

**Acceptance:** 行为不变，类型检查和表单回归通过，文件职责更单一。

### Task P3-02：统一新增面板只挂载当前活动表单

**Files:**

- Modify: `src/components/UnifiedReminderComposer/index.tsx:102`

**证据：** 药品和检查表单始终同时挂载，只用 `display:none` 隐藏非活动面板，两套 store 订阅和初始化 effect 同时运行。

- [ ] 只渲染活动表单。
- [ ] 明确切换类型时的草稿策略：沿用当前重置语义；若保留草稿，只提升最小表单值，不保留隐藏组件实例。
- [ ] 用 React Profiler 或挂载计数确认一次只存在一个表单。

**Acceptance:** 打开新增面板只挂载当前表单，切换和提交行为保持一致。

### Task P3-03：清理未使用存储代码、资源和误导性配置

**Files:**

- Review/Remove: `src/utils/storage.ts:6-24`、`src/constants/index.ts:157-159`
- Review/Remove: 未被源码引用的多份 logo 与已移除 checkup Tab 图标
- Simplify: `config/dev.ts`、`config/prod.ts`

**证据：** `loadReminders/saveReminders` 仅定义未调用；多份 logo 合计约 3 MB 且没有源码引用；dev/prod 有效配置等价，prod 保留与当前 Vite 不符的 Webpack 注释模板。

- [ ] 先用 `rg`、构建资源清单和小程序配置确认无动态引用，再删除死代码/资源。
- [ ] 保留分享图和当前四 Tab 实际使用的 PNG；不要因存在 SVG 就替换当前小程序配置。
- [ ] 若 dev/prod 暂无差异，简化为空扩展点并删除误导性 Webpack 注释。

**Acceptance:** 构建产物和 UI 不变，仓库体积下降，配置不再暗示未启用能力。

### Task P3-04：减少三个提醒云函数的重复实现

**Files:**

- Create: `cloudfunctions/cloud1-d3gqjwfefe40e4dba/functions/shared/`
- Modify: `reminder/index.js`、`expiryReminder/index.js`、`checkupReminder/index.js`

- [ ] 仅抽取已在 P1-02/P1-03 验证稳定的时间、微信请求、token 缓存、用户资格和日志辅助函数。
- [ ] 业务候选规则仍留在各自函数，避免过度抽象。
- [ ] 确认云函数部署工具会把 shared 代码打包；若不会，使用部署前复制脚本并校验哈希，不依赖运行时跨函数路径。

**Acceptance:** 三个函数的公共行为由一套测试约束，独立部署仍可运行。

---

## 6. 建议执行顺序与里程碑

### Milestone A：恢复提醒可信度

1. P0-01 过期提醒调度。
2. P0-02 历史药品兼容。
3. P1-01 模板级授权。
4. P1-02 时区统一。
5. P1-03 并发防重与失败告警。

完成标准：用测试环境数据分别收到开药、过期、检查三类提醒；调度失败可告警；无重复发送。

### Milestone B：保证数据与界面一致

1. P1-04 待盘点状态。
2. P1-05 检查完成并发。
3. P1-06 健康指标一致性。
4. P1-07 加载状态。
5. P1-08 历史迁移。
6. P1-09 输入与身份边界。

完成标准：断网、慢网、并发、旧数据和跨端操作都不会产生假成功、重复记录或错误空态。

### Milestone C：建立持续交付基线

1. P1-10 隐私日志。
2. P1-11 统一质量门禁。
3. P1-12 触控与字号。
4. P2-01 至 P2-06。

完成标准：全新环境执行 `pnpm run verify` 通过；375/414 宽真机关键交互通过；OpenSpec 与文档同步。

### Milestone D：低风险维护

执行 P3 任务。每项单独提交，不与业务修复混合。

---

## 7. 每个任务的统一完成门槛

- [ ] 有失败用例或可重复的复现步骤，再实施最小修复。
- [ ] `pnpm run typecheck` 通过。
- [ ] 相关逻辑验证、云函数测试或组件交互测试通过。
- [ ] `pnpm run lint` 与 `pnpm run lint:styles` 只读通过。
- [ ] loading、empty、error、输入错误和重复点击场景已覆盖。
- [ ] 不包含 openid、药名、昵称、头像、备注等敏感日志。
- [ ] `git diff --check` 通过，且没有无关文件改动。
- [ ] 若因微信云开发、真实订阅额度或真机限制无法验证，任务记录必须明确剩余风险和人工验证步骤。
