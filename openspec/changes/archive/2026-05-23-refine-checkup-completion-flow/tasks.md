## 1. 完成流程数据与状态

- [x] 1.1 梳理 `completeCheckup` 当前调用点，确认首页、检查列表和详情页的完成入口都能改为统一面板入口。
- [x] 1.2 规范 `completeCheckup` 入参和状态流转：传入 `doneDate`、可选 `note`、可选 `nextTargetDate`，并由 `nextTargetDate` 决定 `active` 或 `done`。
- [x] 1.3 增加完成日期和下一次检查日期校验，确保实际完成日期不晚于今天，下一次检查日期晚于实际完成日期。

## 2. 统一完成检查面板

- [x] 2.1 新增检查完成 BottomSheet 组件，包含实际完成日期、结果备注、下一次检查日期和两个完成动作。
- [x] 2.2 复用当前医疗绿色、浅卡片和 BottomSheet 视觉语言，参考开药逾期完成面板的文案密度和日期选择交互。
- [x] 2.3 在面板内处理保存、校验提示、成功提示和失败提示，保存成功后关闭面板并刷新当前视图状态。

## 3. 入口接入

- [x] 3.1 将首页检查主操作从确认弹窗改为打开统一完成检查面板。
- [x] 3.2 将检查 Tab 列表卡片的「完成检查」操作改为打开统一完成检查面板。
- [x] 3.3 将检查详情页的完成入口改为打开统一完成检查面板，并确保暂停、编辑、删除入口继续可用。

## 4. 详情页与历史展示

- [x] 4.1 从检查详情页移除常驻「完成本次检查」表单，保留明确的完成检查按钮和完成历史。
- [x] 4.2 确认完成历史展示实际完成日期和备注；没有备注时保留默认完成描述。
- [x] 4.3 确认「完成并设下次」后详情和列表展示为有效状态，并使用新的检查日期重新计算风险等级。

## 5. 验证

- [x] 5.1 验证逾期检查从首页完成时可以选择实际完成日期，并且不会静默按点击当天写死。
- [x] 5.2 验证「完成不设下次」进入已完成状态，「完成并设下次」保持有效状态且不再显示已完成。
- [x] 5.3 验证未来完成日期和无效下一次日期会被拦截并展示提示。
- [x] 5.4 执行 TypeScript 检查，确认新增组件、入口状态和 `completeCheckup` 调用类型无误。

## 6. 已完成提醒重新安排

- [x] 6.1 在已完成检查详情中增加「重新安排检查」主动作，仅保留删除入口，不再展示编辑提醒入口。
- [x] 6.2 新增轻量重新安排面板，仅填写下一次检查日期、提前提醒和提醒时间。
- [x] 6.3 保存重新安排时将检查状态恢复为有效，清空提醒去重字段，并保留完成历史。
- [x] 6.4 执行 TypeScript 检查，确认重新安排入口和面板类型无误。
