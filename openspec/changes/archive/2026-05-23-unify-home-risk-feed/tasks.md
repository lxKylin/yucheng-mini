## 1. 首页待办派生

- [x] 1.1 梳理当前 `src/pages/home/index.tsx` 中开药提醒、检查摘要和统计数据的使用点。
- [x] 1.2 新增或调整首页派生逻辑，将 `useDerivedList()` 与检查提醒派生数据合并为统一首页待办视图模型。
- [x] 1.3 实现统一排序规则：已逾期、今日到期、7 天内或提醒窗口内、更远有效事项，同级按目标日期从近到远。
- [x] 1.4 确保暂停、已完成和已删除的开药提醒或检查提醒不进入首页待处理风险统计。

## 2. 首页 UI 调整

- [x] 2.1 将首页「最近提醒」模块调整为「优先待办」，并更新副文案为风险排序语义。
- [x] 2.2 使用统一待办列表渲染首页最多展示项，避免先固定截取 3 条开药提醒。
- [x] 2.3 为首页待办项增加「开药」或「检查」类型标识，并保留对应业务操作入口。
- [x] 2.4 保留进入提醒列表 Tab 和检查 Tab 的完整管理入口。
- [x] 2.5 调整首页空状态，确保没有紧急检查时不产生虚假检查风险卡片。

## 3. 检查 Tab 结构调整

- [x] 3.1 移除或弱化检查 Tab 顶部大型独立 Hero 模块。
- [x] 3.2 将检查 Tab 首屏结构调整为搜索、状态筛选、类型筛选和列表内容优先。
- [x] 3.3 如仍需展示统计信息，改用筛选计数或轻量统计区域。
- [x] 3.4 确保检查提醒的搜索、筛选、新增、查看详情和完成操作保持可用。

## 4. 交互与样式校准

- [x] 4.1 调整首页和检查 Tab 样式，使其延续现有医疗绿色、浅色卡片和底部导航风格。
- [x] 4.2 检查不同屏幕高度下首页优先待办不会被底部导航或悬浮新增按钮遮挡。
- [x] 4.3 确认首页待办卡片的类型标识、状态文案和主操作在移动端不发生文字重叠。

## 5. 验证

- [x] 5.1 使用包含逾期开药、未来开药、逾期检查和无检查风险的样例数据验证首页排序。
- [x] 5.2 验证当开药提醒数量达到首页展示上限时，更紧急的检查提醒仍能进入首页展示。
- [x] 5.3 执行 TypeScript 检查，重点确认首页旧摘要变量没有残留引用。
- [x] 5.4 如构建环境可用，执行小程序构建或等价本地验证，并记录任何环境阻塞。
