## 1. 数据模型与工具

- [x] 1.1 新增检查提醒类型、状态/类型常量和完成历史类型。
- [x] 1.2 新增检查提醒日期派生工具，覆盖检查日期、提醒日期、剩余天数、紧急等级和排序。
- [x] 1.3 新增云端检查记录迁移/规范化工具，并提供安全默认值。

## 2. 云服务与状态管理

- [x] 2.1 实现 `src/services/checkup.ts`，支持针对 `checkups` 集合的列表、新增、更新和软删除操作。
- [x] 2.2 实现 Zustand 检查 store，支持加载、新增、更新、删除、暂停/恢复和完成操作。
- [x] 2.3 新增 React hooks，提供派生检查列表、单条记录查询、首页摘要和 store actions。
- [x] 2.4 在用户认证后的应用启动流程中加载检查提醒，且不改变现有药品加载行为。

## 3. 检查 Tab UI

- [x] 3.1 新增 `pages/checkups/index` 路由、页面配置、SCSS 和「检查」tabBar 项。
- [x] 3.2 新增或接入「检查」Tab 的默认态和选中态图标。
- [x] 3.3 实现「检查」Tab 列表页，包含搜索、类型筛选、状态筛选、紧急排序和空状态。
- [x] 3.4 实现可复用检查卡片，展示标题、类型、检查日期、紧急标签、关联药品、医院和下一步操作。

## 4. 检查表单与详情流程

- [x] 4.1 实现 BottomSheet 内的检查新增/编辑表单，包含标题、类型、检查日期、提前天数、提醒时间、医院、备注和可选关联药品。
- [x] 4.2 增加必填标题和有效计划字段校验，并提供用户可见反馈。
- [x] 4.3 实现检查详情视图，展示提醒设置、关联药品、医院、备注和完成历史。
- [x] 4.4 在详情流程中实现暂停/恢复和软删除操作。
- [x] 4.5 实现完成流程，记录历史，并支持“不设置下一次日期直接完成”或“设置下一次检查日期”。

## 5. 首页集成

- [x] 5.1 新增检查首页摘要派生逻辑，统计逾期和今日到期检查。
- [x] 5.2 更新首页风险/待办区域，纳入紧急检查提醒，同时保留现有开药提醒。
- [x] 5.3 确保暂停、已完成和已删除的检查提醒不会产生虚假首页紧急提示。

## 6. 云端提醒推送

- [x] 6.1 新增定时云提醒函数，或在现有云函数中增加独立分支，用于扫描有效检查提醒。
- [x] 6.2 实现检查订阅消息 payload 映射、当天重复发送拦截和订阅授权状态处理。
- [x] 6.3 增加云函数配置，并记录检查提醒推送所需的环境变量。
- [x] 6.4 明确 `checkups` 查询需要的云数据库索引，包括用户、状态、提醒日期和最近发送日期。

## 7. 验证

- [ ] 7.1 验证现有药品开药提醒的创建、列表展示、已开药和云提醒行为仍然可用。
- [ ] 7.2 验证独立检查提醒和关联药品检查提醒可以创建、编辑、筛选、完成、暂停、恢复和删除。
- [x] 7.3 使用项目兼容的 TypeScript 命令进行类型验证。
- [x] 7.4 运行或记录云函数本地检查，覆盖到期、跳过、重复发送和暂停检查提醒场景。
