## 1. 首页待办展示模型

- [x] 1.1 梳理 `useHomeRiskFeed` 当前输出字段，确认首页轻量卡片需要的标题、类型、风险、检查日期、提醒时间和辅助信息来源。
- [x] 1.2 如有必要，在首页派生层补充仅供首页展示使用的辅助文案字段，不修改 `Medicine` 或 `CheckupReminder` 核心模型。

## 2. 首页轻量待办卡片

- [x] 2.1 新增或提取首页专用轻量待办卡片，用统一布局展示开药和检查事项，使用 Frontend Design 以及 UI Ux Pro Max 这两个 Skill 进行辅助设计。
- [x] 2.2 替换首页优先待办列表中直接嵌入的完整 `MedicineCard` 和 `CheckupCard`。
- [x] 2.3 保留开药「已开药 / 查看详情」和检查「已完成 / 查看详情」操作语义，并确保主操作不会同时触发卡片详情或跳转。
- [x] 2.4 调整首页待办样式，使风险状态跨类型一致，业务类型标签只表达「开药 / 检查」来源。

## 3. 首页空状态与新增入口

- [x] 3.1 调整无任何记录时的首页空状态文案，明确右下角 `+` 用于新增开药提醒。
- [x] 3.2 在无任何记录的空状态中提示检查/复诊提醒前往「检查」页新增。
- [x] 3.3 调整已有记录但暂无待办时的空状态文案，避免暗示首页 `+` 可以新增检查提醒。
- [x] 3.4 确认首页右下角 `+` 仍打开开药提醒新增流程，不新增首页多类型新增弹窗。

## 4. 验证

- [x] 4.1 验证首页同时存在开药和检查待办时，两类事项使用统一轻量卡片展示且风险视觉一致。
- [x] 4.2 验证首页开药主操作、检查主操作和详情/跳转入口互不误触。
- [x] 4.3 验证无记录和有记录但暂无待办两种空状态文案符合规格。
- [x] 4.4 运行 TypeScript 检查，至少覆盖首页、`useHomeRiskFeed` 和新增或调整的首页卡片代码。
