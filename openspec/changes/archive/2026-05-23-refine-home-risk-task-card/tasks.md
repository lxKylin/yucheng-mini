## 1. 首页待办展示字段

- [x] 1.1 梳理 `HomeRiskFeedItem` 当前字段，确认风险文案、标题、类型、日期、动作所需信息都可从现有数据得到。
- [x] 1.2 为首页待办补充或派生风险前置文案，例如逾期天数、今天到期、未来天数。
- [x] 1.3 为开药和检查分别补充首页行动摘要，避免只展示规格、周期、医院等字段拼接。
- [x] 1.4 为目标日期和提醒时间补充轻量时间摘要，保留必要日期信息但避免两个大日期块。
- [x] 1.5 为开药和检查补充明确主操作文案，确保文案适合移动端宽度。

## 2. 首页风险任务卡片

- [x] 2.1 调整 `HomeRiskCard` 信息顺序，使风险状态位于首层，业务类型作为来源标签。
- [x] 2.2 将标题、行动摘要、轻量时间摘要按首页待办层级渲染，移除两个并列日期大块。
- [x] 2.3 保持开药和检查使用同一结构，仅在摘要文案、类型标签和主操作文案上体现差异。
- [x] 2.4 调整卡片点击、详情入口和主操作事件边界，确保主操作不会触发详情。

## 3. 样式与交互

- [x] 3.1 更新 `src/pages/home/index.scss`，让卡片视觉从管理列表卡片调整为更轻量的风险任务条目。
- [x] 3.2 降低操作区视觉重量，不再使用两个等宽大按钮作为默认布局。
- [x] 3.3 保留足够触控面积和可读性，确保主操作、详情入口、长标题和摘要在移动端不重叠。
- [x] 3.4 复用现有医疗绿色、浅卡片和风险色，不新增独立色彩体系。

## 4. 验证

- [x] 4.1 使用逾期开药、今日开药、逾期检查、未来检查等样例数据检查首页卡片信息层级。
- [x] 4.2 验证首页风险排序和统计数量没有因为展示字段调整而改变。
- [x] 4.3 验证开药主操作、检查主操作和查看详情入口行为仍正确。
- [x] 4.4 执行 TypeScript 检查，重点确认 `src/pages/home/index.tsx` 和 `src/hooks/useHomeRiskFeed.ts` 类型无误。
- [x] 4.5 在可用的小程序或浏览器预览环境中检查首屏布局，确认卡片高度、底部导航和悬浮新增按钮不互相遮挡。
