## 1. 数据与状态闭环

- [x] 1.1 在健康指标云服务中新增软删除指标类型的方法，将目标指标类型状态更新为 `deleted` 并刷新 `updatedAt`
- [x] 1.2 在 `healthMetricStore` 中新增删除指标类型 action，处理提交态、失败态、指标列表移除和当前选中指标切换
- [x] 1.3 确保删除指标类型不级联删除 `healthMetricRecords`，且删除失败时保留当前指标可见状态

## 2. 指标列表排序

- [x] 2.1 增加或调整指标 summary 排序逻辑，按最近记录日期优先、指标更新时间兜底展示
- [x] 2.2 删除当前指标后，按排序后的可展示指标选择下一个指标；无可展示指标时进入空态
- [x] 2.3 补充工具函数或 hook 级验证，覆盖有记录、无记录、同日期记录和删除后列表更新场景

## 3. 页面与交互

- [x] 3.1 在编辑指标 BottomSheet 中增加低强调删除入口，新增指标模式不展示该入口
- [x] 3.2 删除前展示二次确认，文案说明指标将不再展示且历史记录保留
- [x] 3.3 删除成功后关闭表单、展示成功反馈，并刷新指标切换列表、趋势图和最近记录
- [x] 3.4 删除失败时展示错误提示，保留编辑上下文并允许重试

## 4. 验证

- [x] 4.1 运行健康指标工具验证脚本或相关单元验证，确认排序与删除状态处理正确
- [x] 4.2 运行 `pnpm exec tsc --noEmit --ignoreDeprecations 5.0 --skipLibCheck true`
- [x] 4.3 运行 `openspec validate complete-health-metric-management --strict`
- [x] 4.4 手动检查指标追踪页 loading、empty、error、删除确认、删除最后一个指标和删除失败提示等状态
