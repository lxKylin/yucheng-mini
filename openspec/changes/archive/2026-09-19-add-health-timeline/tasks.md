## 1. 数据模型与持久化

- [x] 1.1 新增健康历程事件、事件类型、日期精度、表单及关联字段的 TypeScript 类型和常量，并以纯函数验证年月/年精度展示、稳定排序和治疗时段校验。
- [x] 1.2 新增 `healthTimelineEvents` 云端 service 与迁移/去重逻辑，支持当前用户的分页读取、创建、更新和删除；使用服务层验证远端失败不返回伪成功数据。
- [x] 1.3 新增健康历程 Zustand store 与 hooks，覆盖首屏加载、追加加载、重试、提交和本地列表同步；以 store 场景验证 loading、empty、error、has-more 和保存失败状态。
- [x] 1.4 新增健康历程云数据库集合及 `_openid + occurredAt`、`userId + occurredAt` 索引部署说明，并核对集合名和查询字段与 service 一致。

## 2. 健康历程页面与表单

- [x] 2.1 在健康子包注册健康历程二级页，并在“我的”页记录与复盘区域新增「健康历程」及「记录确诊、检查、治疗与重要变化」入口；验证现有四个主 Tab 与中央新增提醒入口不变。
- [x] 2.2 实现按时间倒序的健康历程时间轴、年份/事件类型筛选、筛选空态和加载更多入口；验证同日事件按更新时间排序且筛选能组合生效。
- [x] 2.3 实现新建和编辑事件的 BottomSheet，覆盖类型、标题、日期精度、备注、可选药品/检查关联，以及治疗类结束日期和进行中状态；验证空标题、未来日期和无效治疗时段均无法提交。
- [x] 2.4 实现删除确认、保存中禁用、保存失败保留表单内容，以及加载、真实空数据、加载失败和重试视图；验证关联对象不可用时仍可查看历史事件。
- [ ] 2.5 补齐页面、时间轴和表单 Sass 样式、触摸目标及 aria 文案，保持单个文件不超过 500 行；在小程序预览中验证长标题、长备注和窄屏无水平溢出。

## 3. 集成验证

- [x] 3.1 运行 `openspec validate --change add-health-timeline --strict`，确认 proposal、design、spec 与 tasks 一致且校验通过。
- [x] 3.2 运行 `pnpm exec tsc --noEmit --ignoreDeprecations 5.0 --skipLibCheck true` 和 `pnpm build:weapp`，确认类型与小程序构建通过；如环境失败，记录完整错误和未覆盖风险。
- [ ] 3.3 手动验收：从“我的”进入健康历程，完成首条事件创建、治疗进行中记录、年份/类型筛选、编辑、删除、加载失败重试和药品/检查只读关联，确认不产生提醒、不改写既有记录且不输出医疗建议。
