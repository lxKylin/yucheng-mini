## 1. 类型与存量兼容

- [x] 1.1 扩展 `Medicine` 类型，增加余量跟踪开关、估算模式、库存基准数量/日期、待盘点状态和最近更新时间字段
- [x] 1.2 扩展 `DerivedMedicine`，增加预计剩余数量、预计可用天数、每日计划用量和库存展示状态等派生字段
- [x] 1.3 更新 `migrateMedicine()`，为存量数据补齐“默认关闭余量跟踪”的安全默认值，并规范化非数字、负数和无效日期
- [x] 1.4 确认旧云端文档无需批量迁移，未开启余量跟踪的药品在首页、提醒列表、提醒详情和药箱中行为不变

## 2. 余量派生与结算规则

- [x] 2.1 新增独立余量工具函数，按基准数量、基准日期、每次用量和每日次数计算每日计划用量、预计剩余数量、预计可用天数及余量状态
- [x] 2.2 实现日级计算口径：基准日不重复扣减、结果不小于 0、数量最多保留两位小数且展示时移除无意义末尾零
- [x] 2.3 实现自动估算资格判断；按医嘱、每日次数为 0、手动模式或待盘点状态不得输出自动预计可用天数
- [x] 2.4 实现剂量变更结算函数，先按旧剂量结算到生效日期，再以结算结果作为新库存基准并应用新剂量
- [x] 2.5 增加余量工具验证脚本或等价单元验证，覆盖半片/四分之一片、基准日、多日消耗、归零、浮点精度、不规则用药和剂量变更边界

## 3. 小数剂量表单

- [x] 3.1 将 `MedicineComposer` 的每次用量改为字符串编辑态，提交时再解析和规范化，确保输入 `0.`、`0.5`、`0.25` 时不被中途清空
- [x] 3.2 为片剂提供 `1/4 片`、`1/2 片`、`1 片`、`1.5 片`、`2 片`快捷选项和自定义输入；非片剂不展示分片文案
- [x] 3.3 增加每次用量校验与错误提示，拦截空值、非数字、不大于 0 或超过两位小数的值
- [x] 3.4 统一剂量格式化展示，使 `0.25`、`0.5` 可显示为 `1/4 片`、`1/2 片`，其他合法小数保持数字文案

## 4. 余量设置与校准表单

- [x] 4.1 在 `MedicineComposer` 增加独立“库存与余量”区块和跟踪开关，不与开药提醒开关绑定
- [x] 4.2 开启余量跟踪后提供当前实际总量、盘点日期和自动/手动估算状态；校验数量不小于 0、最多两位小数、日期不晚于今天
- [x] 4.3 当每日次数为 0 或用药时机为“按医嘱”时，切换或提示使用手动维护，并解释不会自动计算可用天数
- [x] 4.4 从手动维护恢复自动估算时要求重新确认当前实际总量和基准日期
- [x] 4.5 编辑自动估算药品的每次用量或每日次数时展示剂量生效日期，并在保存前执行旧剂量结算
- [x] 4.6 修改用量单位时要求重新盘点与新单位一致的当前总量，不沿用旧库存基准
- [x] 4.7 保持表单提交 loading、防重复提交、取消保护和失败提示；保存失败时保留用户输入及原有可见状态

## 5. Store 与云端更新

- [x] 5.1 在 `reminderStore` 增加库存校准和估算模式切换 action，通过现有 `medicines` 单文档更新链路保存库存基准
- [x] 5.2 扩展药品更新动作，使剂量、剂量生效结算结果和库存基准在一次云端更新成功后再同步本地状态
- [x] 5.3 扩展 `markDone` 或新增等价组合动作，原子处理实际开药日期、开药历史、开药后当前总量和待盘点状态
- [x] 5.4 确认余量跟踪不依赖 `reminderEnabled` 或提醒暂停状态，关闭/暂停开药提醒时仍按用户选择的库存模式工作
- [x] 5.5 补充云端失败场景验证，确保开药日期与库存基准不会在本地出现部分成功状态

## 6. 开药完成流程

- [x] 6.1 将首页、统一提醒列表和提醒详情中的“已开药”入口收敛到同一个可复用 BottomSheet，避免不同入口库存行为不一致
- [x] 6.2 未开启余量跟踪时保持实际开药日期确认和开药历史更新，不强制填写药品总量
- [x] 6.3 已开启余量跟踪时提供“开药后的当前总量”，明确需要包含旧药余量和本次新开药品
- [x] 6.4 支持“稍后盘点”；保存开药日期后将库存标记为待盘点，并停止展示旧的预计剩余量
- [x] 6.5 完善 BottomSheet 日期、数量校验、提交态、失败提示和重试行为；保存失败时保留当前输入

## 7. 药箱展示与筛选

- [x] 7.1 扩展药箱卡片，分别展示自动估算、手动维护、待盘点、余量不足和余量已用尽状态，并提供更新库存入口
- [x] 7.2 自动估算文案必须包含“预计”或“按当前剂量估算”，手动模式不得展示预计可用天数
- [x] 7.3 在药箱增加“余量不足”筛选，按固定 7 天阈值只包含预计可用天数小于等于 7 天或预计余量为 0 的自动估算药品
- [x] 7.4 保持默认药箱列表顺序稳定，不因日期推进或进入余量不足状态自动重排
- [x] 7.5 补齐余量筛选 loading、empty、error 和重试状态，并确认增量列表加载不改变筛选统计和稳定顺序

## 8. 文档与验证

- [x] 8.1 更新 `docs/1.产品设计.md` 和 `docs/3.开发计划.md` 中“近期不做库存管理、药箱不展示余量”的旧边界，补充估算口径与非目标
- [x] 8.2 运行余量派生验证，覆盖 `0.25/0.5` 剂量、基准日、多日消耗、归零、手动模式、待盘点、阈值筛选和剂量变更
- [x] 8.3 运行 `pnpm exec tsc --noEmit --ignoreDeprecations 5.0 --skipLibCheck true`
- [x] 8.4 在 OpenSpec CLI 可用后运行 `openspec validate add-medicine-inventory-estimation --strict` 和 `openspec validate --all --json`
- [x] 8.5 手动验证新增/编辑药品、片剂快捷剂量、自定义小数、开启/关闭余量跟踪、手动校准、剂量调整、用量单位变化和存量药品兼容
- [x] 8.6 分别从首页、提醒列表和提醒详情验证已开药流程，并覆盖填写总量、稍后盘点、取消、重复点击和云端保存失败
- [x] 8.7 检查药箱全部/提醒状态/余量不足筛选、默认稳定顺序、loading、empty、error、触底加载和下拉刷新
