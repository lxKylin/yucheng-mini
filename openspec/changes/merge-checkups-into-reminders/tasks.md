## 1. 导航与入口收敛

- [x] 1.1 更新 `src/app.config.ts`，从底部 TabBar 移除「检查」Tab，保留首页、提醒、药箱、我的四个 Tab
- [x] 1.2 检查 `pages/checkups/index` 的处理策略，确认保留为内部兼容路由或迁移后不再作为主入口
- [x] 1.3 更新首页进入完整提醒管理的跳转逻辑，不再分别跳转提醒 Tab 和检查 Tab
- [x] 1.4 更新首页空状态、入口按钮和相关分享/查询文案，移除对独立「检查」Tab 的提示

## 2. 统一提醒中心数据适配

- [x] 2.1 在提醒中心建立混合提醒项类型，区分 `medicine` 和 `checkup`，并保留原始业务对象
- [x] 2.2 将 `useDerivedList()` 和 `useDerivedCheckups()` 的结果适配为统一列表数据
- [x] 2.3 实现混合列表排序，确保逾期、今日和临近事项按统一风险规则排列
- [x] 2.4 实现跨类型搜索文本构建，支持药品名称/规格、检查事项/医院/关联药品等关键词
- [x] 2.5 保持增量加载 reset key 能响应类型筛选、状态筛选和搜索词变化

## 3. 提醒中心筛选与列表 UI

- [x] 3.1 在提醒中心新增类型筛选：全部、开药、检查
- [x] 3.2 扩展状态筛选：全部、逾期今日、7天内、正常、暂停、已完成
- [x] 3.3 让「已完成」状态筛选仅展示已完成检查提醒，不为开药提醒虚构已完成记录
- [x] 3.4 在同一列表区域渲染 `MedicineCard` 和 `CheckupCard`，并保留清晰的业务类型识别
- [x] 3.5 统一 `MedicineCard` 与 `CheckupCard` 的列表卡片骨架，保持容器、标题层级、状态标签、关键时间、进度提示和底部动作区布局一致
- [x] 3.6 为开药提醒和检查提醒都提供显式「查看详情」入口，主操作分别保留「已开药」和「完成检查」
- [x] 3.7 确认风险颜色继续跟随现有逾期、临近、正常、暂停或已完成状态，不用业务类型颜色覆盖风险语义
- [x] 3.8 更新空状态文案，区分无任何提醒、当前筛选无结果、只有某一类型无结果

## 4. 统一新增提醒 Sheet

- [x] 4.1 新增或抽取统一新增提醒 Sheet 组件，标题使用「新增提醒」或等价文案
- [x] 4.2 在 Sheet 顶部实现开药提醒 / 检查提醒分段切换控件
- [x] 4.3 默认选中开药提醒，并渲染 `MedicineComposer`，且 `defaultReminderEnabled=true`
- [x] 4.4 切换到检查提醒时渲染 `CheckupComposer`
- [x] 4.5 处理已填写内容时切换类型的保护逻辑，采用确认提示或独立草稿保留策略
- [x] 4.6 保存成功后关闭 Sheet、刷新对应列表状态，并保持当前筛选下的展示一致
- [x] 4.7 首页右下角 `+` 复用统一「新增提醒」Sheet，与提醒 Tab 新增按钮保持一致

## 5. 编辑与详情流程

- [x] 5.1 开药提醒详情和编辑继续复用 `ReminderDetail`、`MedicineComposer` 和已开药流程
- [x] 5.2 检查提醒详情和编辑接入统一提醒中心，复用 `CheckupDetail` 和 `CheckupComposer`
- [x] 5.3 检查提醒完成流程接入统一提醒中心，复用 `CheckupCompletionSheet`
- [x] 5.4 检查提醒重新安排流程接入统一提醒中心，复用 `CheckupRestartSheet`
- [x] 5.5 编辑已有开药或检查提醒时锁定业务类型，不展示类型切换控件

## 6. 独立业务域与云端链路保护

- [x] 6.1 确认创建检查提醒仍写入检查提醒数据域，不修改药品开药提醒字段
- [x] 6.2 确认创建开药提醒仍写入药品数据域，不创建检查提醒记录
- [x] 6.3 确认 `checkupStore`、`checkup` service、`useCheckups` 和 `checkupReminder` 云函数链路不被合并进药品提醒链路
- [x] 6.4 确认药箱页仍只展示药品资料和开药提醒开关，不展示检查提醒

## 7. 文档同步

- [x] 7.1 更新 `docs/1.产品设计.md`，将独立检查 Tab 描述调整为统一提醒中心内的检查提醒能力
- [x] 7.2 更新 `docs/3.开发计划.md`，同步四 Tab 导航、统一提醒中心和新增提醒表单内类型切换方案
- [x] 7.3 检查文档中的「检查 Tab」「检查页」「前往检查」等旧表述，保留必要历史说明但避免作为当前主路径

## 8. 验证

- [x] 8.1 运行 TypeScript 类型检查，确认统一提醒中心、Sheet 状态和组件复用无类型错误
- [ ] 8.2 手动验证底部导航只展示首页、提醒、药箱、我的四个 Tab
- [ ] 8.3 手动验证统一提醒中心在全部、开药、检查类型筛选下的展示正确
- [ ] 8.4 手动验证逾期今日、7天内、正常、暂停、已完成状态筛选正确
- [ ] 8.5 手动验证开药提醒和检查提醒卡片在混合列表中的视觉骨架、详情入口和主操作位置一致
- [ ] 8.6 手动验证新增提醒 Sheet 默认展示开药表单，切换后展示检查表单，且不出现先弹选择对话框
- [ ] 8.7 手动验证编辑开药提醒和编辑检查提醒时不展示类型切换控件
- [ ] 8.8 手动验证检查提醒详情、完成检查、重新安排流程在统一提醒中心可用
- [ ] 8.9 手动验证首页风险卡片、完整管理入口和空状态不再依赖独立检查 Tab
- [ ] 8.10 手动验证药箱页、开药提醒云函数和检查提醒云函数行为未因入口合并而改变
