## 1. 共享列表加载行为

- [x] 1.1 新增共享常量，定义首批展示数量为 10、每次追加数量为 10。
- [x] 1.2 实现可复用的增量列表 hook 或工具，返回可见列表项、总数量、已展示数量、是否还有更多，以及加载更多动作。
- [x] 1.3 确保搜索词或筛选依赖变化时，共享增量逻辑会重置为首批展示数量。

## 2. 下拉刷新接入

- [x] 2.1 在「提醒」「药箱」「检查」三个页面配置中启用下拉刷新。
- [x] 2.2 为「提醒」页接入刷新处理，复用现有开药提醒云端加载路径。
- [x] 2.3 为「药箱」页接入刷新处理，复用现有药品云端加载路径。
- [x] 2.4 为「检查」页接入刷新处理，复用现有检查提醒云端加载路径，并在关联药品名称需要保持最新时刷新药品数据。
- [x] 2.5 确保所有刷新路径在成功或失败后都会停止下拉刷新动画，并在加载失败时展示失败反馈。

## 3. 标签页增量渲染

- [x] 3.1 更新「提醒」页，只渲染当前可见的增量结果，同时保留现有筛选、数量统计、卡片操作和空态行为。
- [x] 3.2 更新「药箱」页，只渲染当前可见的增量结果，同时保留现有筛选、数量统计、编辑/删除流程和空态行为。
- [x] 3.3 更新「检查」页，只渲染当前可见的增量结果，同时保留状态/类型筛选、数量统计、详情/完成流程和空态行为。
- [x] 3.4 增加页面触底处理，让三个页面追加下一批可见结果，不发起云端分页请求。
- [x] 3.5 增加轻量底部状态，用于展示已显示数量、总数量、加载更多提示和没有更多状态。

## 4. 样式与交互打磨

- [x] 4.1 为三个页面的新底部状态补充样式，使其匹配当前医疗绿色、浅色卡片的视觉语言。
- [x] 4.2 确保小屏下底部状态文案不会和悬浮新增按钮或底部标签栏重叠。
- [x] 4.3 验证搜索词或筛选条件变化后，列表会回到首批 10 条可见结果。

## 5. 验证

- [x] 5.1 如果本地测试环境支持，为共享增量列表逻辑增加聚焦测试或轻量验证覆盖。
- [x] 5.2 使用项目现有的 no-emit TypeScript 检查命令进行类型验证。
- [x] 5.3 手动验证三个页面在超过 10 条、少于 10 条、空结果、刷新成功/失败和多次触底加载场景下的表现。
- [x] 5.4 确认实现没有引入云端分页、数据模型变更或排序语义变更。
