## 1. 数据模型与纯函数

- [x] 1.1 在类型定义中新增指标类型、指标记录、指标状态、周趋势点、记录表单和摘要相关 TypeScript 类型
- [x] 1.2 新增指标追踪常量，包含默认状态、数值显示精度、日期窗口长度、表单默认值和可复用文案
- [x] 1.3 新增指标追踪工具函数，覆盖名称归一化、日期窗口计算、数值格式化、参考范围判断、同日记录匹配和记录排序
- [x] 1.4 为指标工具函数补充轻量单元或类型级验证用例，至少覆盖参考范围、未来日期和同名指标判断

## 2. 云端服务与状态管理

- [x] 2.1 新增指标追踪云服务，支持加载指标类型、加载日期范围记录、创建指标类型、保存记录、查找同日同指标记录和更新指标默认设置
- [x] 2.2 新增指标追踪 Zustand store，维护指标类型、记录缓存、当前选中指标、loading/error/submitting 状态
- [x] 2.3 新增指标追踪 hooks，暴露当前指标、趋势点、最近记录、空态判断、重试加载、创建指标和保存记录动作
- [x] 2.4 在应用启动或指标页进入时接入指标数据加载，避免增加首页首屏不必要负担
- [x] 2.5 新增云数据库索引说明文档，覆盖指标类型集合和指标记录集合的用户、指标、日期查询索引

## 3. 指标追踪页面

- [x] 3.1 新增「指标追踪」二级页面和页面配置，并在 `src/app.config.ts` 注册路由但不加入底部 Tab
- [x] 3.2 实现指标追踪页 loading、empty、error 和有数据状态，确保每种状态都有明确反馈和重试/新增入口
- [x] 3.3 实现指标切换区，默认选中最近记录过的指标，并支持用户切换当前指标
- [x] 3.4 实现最近 7 次趋势图组件，展示折线、日期、直接数值标签、Y 轴顶部单位、参考范围提示和无记录空态
- [x] 3.5 按当前指标最近记录自动刷新趋势图和记录摘要，不再使用周窗口切换
- [x] 3.6 实现最近记录列表，展示日期、数值、单位、参考范围状态和备注摘要

## 4. 新增指标与记录流程

- [x] 4.1 实现新增指标类型 BottomSheet，支持输入指标名称、单位、参考下限、参考上限和校验错误提示
- [x] 4.2 实现新增指标类型成功后的首次记录衔接，创建后直接进入锁定该指标的记录表单
- [x] 4.3 实现记录已有指标 BottomSheet，从当前指标进入时锁定指标名称并默认带出单位和参考范围
- [x] 4.4 实现没有当前指标时的记录前置选择列表，提供已有指标选择和新增指标类型入口，不区分常用/自定义
- [x] 4.5 实现记录表单校验，覆盖空数值、非数字数值、未来日期、无效参考范围和保存失败重试
- [x] 4.6 实现同日同指标已有记录时的更新确认，确认后更新原记录而不是创建重复记录
- [x] 4.7 实现单位/参考范围修改选项，支持仅本次记录使用或保存为该指标默认设置

## 5. 个人页入口与边界打磨

- [x] 5.1 在个人页新增「指标追踪」低频入口，并与现有近一个月状态记录入口保持同一复盘层级
- [x] 5.2 确认底部自定义 TabBar 仍只展示「首页」「提醒」「药箱」「我的」，中央 `+` 仍只打开统一新增提醒入口
- [x] 5.3 确认首页不新增指标小卡，也不把指标异常写入首页风险中心
- [x] 5.4 为指标页面和 BottomSheet 补齐触摸目标、aria-label、错误提示位置、提交 loading 和取消/关闭行为
- [x] 5.5 确保页面和组件文件保持 500 行以内，必要时拆分趋势图、表单、列表和入口组件

## 6. 验证

- [x] 6.1 运行 `openspec validate --changes add-health-metric-tracking --json`
- [x] 6.2 运行 `pnpm exec tsc --noEmit --ignoreDeprecations 5.0 --skipLibCheck true`
- [ ] 6.3 手动验收无指标空态：从个人页进入指标追踪页，创建新指标后自动进入首次记录
- [ ] 6.4 手动验收已有指标记录：从当前指标点击「+ 记录」时指标名称锁定，保存后趋势图和最近记录刷新
- [ ] 6.5 手动验收同日同指标更新：重复保存同一日期记录时出现确认，确认后更新原记录
- [ ] 6.6 手动验收趋势图：最近 7 次记录直接显示数值，Y 轴顶部显示单位，低频记录不因当前周无数据而隐藏
- [x] 6.7 若 `pnpm run dev:weapp` 或 `pnpm run build:weapp` 因本地 Taro/Rust 环境失败，记录完整错误和剩余风险

  记录：`pnpm run build:weapp` 启动 Taro v4.2.0 后，本地 Rust 依赖 `system-configuration-0.5.1/src/dynamic_store.rs:154:1` panic：`Attempted to create a NULL object.`；进程随后无业务编译输出并被结束，最终 `ELIFECYCLE Command failed`。剩余风险：需在微信开发者工具或可用的本机构建环境中完成 6.3-6.6 的交互验收。
